/* ft_sort_int_tab.c */
#include "../libft.h"

void	ft_sort_int_tab(int *tab, int size)
{
	// TODO: Trier le tableau par ordre croissant
	// Algorithme suggéré: tri par sélection (selection sort)
	// - Boucle externe: i de 0 à size-1
	// - Boucle interne: j de i+1 à size
	// - Si tab[i] > tab[j], utiliser ft_swap(&tab[i], &tab[j])
	(void)tab;
	(void)size;
}
